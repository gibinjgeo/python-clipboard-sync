# Supabase Python client — used by cloud_client.py
supabase>=2.0.0

# Clipboard fallback for non-Wayland Linux (X11 / no wl-clipboard binary)
pyperclip>=1.8.2

# Windows clipboard (Windows only)
pywin32>=306; sys_platform == "win32"
